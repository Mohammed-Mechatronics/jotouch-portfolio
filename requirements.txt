# JoTouch Portfolio — core dependencies
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

# Computer vision / hand tracking
opencv-python>=4.8
mediapipe>=0.10

# Web UI / API
fastapi==0.136.3
uvicorn[standard]==0.48.0
python-multipart==0.0.20

# Serial + hardware
pyserial>=3.5
websockets>=12.0

# Dashboard
streamlit>=1.28
plotly>=5.17

# Report generation
jinja2>=3.1

# Testing / notebook exploration
pytest>=7.4
pytest-timeout>=2.0
httpx>=0.27.0
jupyter>=1.0

# ML data pipeline (Parquet + split specs)
pyarrow>=14.0
pyyaml>=6.0

# LED synchronization (FFT xcorr — default method)
numba>=0.59

# LED synchronization (legacy hybrid PRBS + NAd — optional)
# Uncomment if you need to run the legacy sync algorithm:
# nearest-advocate>=0.2.0
